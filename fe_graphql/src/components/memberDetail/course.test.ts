/*import React from 'react';
import { shallow } from 'enzyme';

import Course from './course';

describe('Course', () => {
  it('should render correctly in "debug" mode', () => {

    const component = shallow(<Course debug />);
  
    expect(component).toMatchSnapshot();
  });
});*/